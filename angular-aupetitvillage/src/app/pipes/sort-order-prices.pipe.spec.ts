import { SortOrderPricesPipe } from './sort-order-prices.pipe';

describe('SortOrderPricesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortOrderPricesPipe();
    expect(pipe).toBeTruthy();
  });
});
